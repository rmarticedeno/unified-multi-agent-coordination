\begin{tikzpicture}[
  node distance=9mm and 7mm,
  every node/.style={font=\scriptsize},
  box/.style={draw=black!75, rounded corners=2pt, align=center, minimum height=9mm, inner sep=3pt},
  linguistic/.style={box, fill=black!6, text width=22mm},
  symbolic/.style={box, fill=black!17, text width=22mm},
  execution/.style={box, fill=white, densely dashed, text width=22mm},
  flow/.style={-Latex, semithick},
  feedback/.style={-Latex, semithick, dashed},
  lane/.style={draw=black!50, dashed, rounded corners=2pt, inner sep=5pt}
]
\node[linguistic] (utterance) {Expresión de usuario $u$};
\node[linguistic, right=of utterance] (problem) {Problema candidato $\widehat{P}$};
\node[symbolic, below=15mm of problem] (problemcheck) {Validación del\\contrato del problema};
\node[symbolic, right=of problemcheck] (boundary) {Frontera operacional\\$\mathcal{B}(\mathcal{R})$};
\node[symbolic, right=of boundary] (authorize) {Viabilidad y\\autorización};
\node[symbolic, right=of authorize] (decision) {$\Pi$ o\\inviabilidad $I$};
\node[linguistic, above=15mm of authorize] (candidate) {Plan candidato $\widehat{\Pi}$};
\node[linguistic, above=15mm of decision] (explanation) {Decisión legible\\para humanos};

\node[execution, below=15mm of decision] (commitments) {Compromisos};
\node[execution, left=of commitments] (participants) {Agentes participantes};
\node[execution, left=of participants] (observations) {Artefactos y\\observaciones};
\node[execution, left=of observations] (structured) {Retroalimentación\\estructurada};

\draw[flow] (utterance) -- node[above]{T1} (problem);
\draw[flow] (problem) -- (problemcheck);
\draw[flow] (problemcheck) -- (boundary);
\draw[flow] (boundary) -- node[right]{T2} (candidate);
\draw[flow] (candidate) -- (authorize);
\draw[flow] (authorize) -- node[above]{T3} (decision);
\draw[flow] (decision) -- (explanation);
\draw[flow] (decision) -- (commitments);
\draw[flow] (commitments) -- (participants);
\draw[flow] (participants) -- (observations);
\draw[flow] (observations) -- (structured);
\draw[feedback] (structured.north) -- node[left]{T4} (problemcheck.south);

\begin{scope}[on background layer]
  \node[lane, fit=(utterance)(problem)(candidate)(explanation), label=above left:{\scriptsize Carril lingüístico}] {};
  \node[lane, fit=(problemcheck)(boundary)(authorize)(decision), label=above left:{\scriptsize Carril simbólico}] {};
  \node[lane, fit=(structured)(observations)(participants)(commitments), label=below left:{\scriptsize Ejecución distribuida y retroalimentación}] {};
\end{scope}
\end{tikzpicture}
