\chapter{Conclusions and Future Work}
\label{chap:conclusion}

The thesis proposes a protocol-adaptable boundary between linguistic proposal and executable multi-agent coordination. RQ1 is answered at the design and prototype level: canonical capabilities, typed constraints, task dependencies, authority, validation and completion contracts, artifacts, attempts, and persisted trace identity must be explicit to justify dispatch. RQ2 is answered architecturally: language interprets and proposes, while deterministic components resolve identity, evaluate enforceable predicates, authorize, refuse, and record. RQ3 is answered through bounded task-local auxiliary specifications whose method, schema, validator, read-only authority, and lifecycle are explicit.

RQ4 is not yet answered by the corrected comparative study. The 36-case corpus, three-model/five-seed collector, configuration scorer, paired tests, and provenance records are implemented, but independent label approval has not been supplied and collection is correctly blocked. No legacy numerical result is substituted. The current evidence supports prototype behavior in unit, deterministic, Docker A2A, official-sample, and PostgreSQL crash-fault checks; it does not support broad empirical superiority.

The principal scientific claim is therefore deliberately narrow. The implementation demonstrates how established symbolic, security, persistence, and interoperability mechanisms can be integrated so that unsupported linguistic proposals are refused before action and process evidence survives recovery. It does not prove semantic truth, production safety, malicious-agent resistance, decentralized consensus, Byzantine tolerance, or universal protocol interoperability.

Before submission, future work is operational rather than speculative: raise critical-module coverage to the declared gates; obtain independent corpus sign-off; collect and preserve all 540 outputs; execute the paired analysis and ablations; regenerate every thesis table from frozen reports; supply tutor, tribunal, date, commit, and repository metadata; compile and visually inspect the PDF; and freeze source, corpus, evidence, and thesis at one clean tagged commit. Longer-term research should add cryptographic admission, adversarial registries, independently developed domain agents, semantic validators, alternate-agent replanning, and broader deployment populations.
