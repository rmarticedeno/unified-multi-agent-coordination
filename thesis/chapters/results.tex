\chapter{Evaluation and Results}
\label{chap:results}

\section{Evidence Status}

This chapter distinguishes historical version 0.2--0.4 evidence, the frozen version~0.5 primary comparison, and the consensus-backed core evaluation track. Earlier reports are development artifacts and are excluded from the final RQ4 comparison. The v0.5 corpus is author-labelled, pre-specified, frozen, and label-hidden during inference; no independent adjudicator was available. At the evidence-candidate freeze represented by this text, the new 1,440-output matrix and clean 33-trial consensus campaign remain release gates. Consequently, historical numbers below describe development behavior and cannot substitute for the final v0.5/RQ5 results.

\begin{table}[htbp]
\centering
\caption{Repository evidence status after the distributed implementation update.}
\label{tab:evidence-status}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.44\textwidth}>{\raggedright\arraybackslash}p{0.18\textwidth}>{\raggedright\arraybackslash}p{0.27\textwidth}}
\toprule
\textbf{Gate} & \textbf{Observed} & \textbf{Required} \\
\midrule
Python tests & \EvidenceTestCount{} passing & all passing \\
Overall production branch coverage & \ProductionBranchCoverage\% & at least 85\% \\
Deterministic scenarios & 7/7 & 7/7 \\
Docker A2A checks & 11/11 & 11/11 \\
PostgreSQL distributed checks & 5/5 & 5/5 \\
Historical etcd three-voter smoke checks & 7/7 & historical only \\
Full 3/5/7 consensus campaign & not collected & 33 accounted trials; criteria reported \\
Upstream-derived A2A interoperability & 1/1 & passing \\
Ruff and mypy & passing & passing \\
Frozen author-label provenance & complete & complete \\
Historical v0.4 outputs & 540/540 & historical only \\
Primary v0.5 outputs & \VFiveOutputCount/1,440 & 1,440/1,440 \\
Deterministic schema 0.5.0 analysis & pending & hash-reproducible \\
\bottomrule
\end{tabular}
\end{table}

Branch-aware production coverage is \ProductionBranchCoverage\%, above the 85\% aggregate gate. Every independently enforced critical-module threshold passes, including 90\% for cluster discovery, at least 80\% for the consensus campaign driver, and at least 90\% for evidence preflight. All \EvidenceTestCount{} tests, Ruff, mypy, the production aggregate, and the module-specific gates pass on the recorded Windows run. This closes the automated code-quality gate, but it does not substitute for the missing clean v0.5 collection and consensus campaign.

\section{Fail-Closed Symbolic Boundary}

Regression tests show that a promised artifact does not make a validatorless task verifiable; legacy textual constraints become unresolved typed constraints and force refusal; missing JSON-pointer paths, false predicates, canonical capability-ID mismatch, incomplete mode coverage, and nested JSON Schema type violations are rejected. Effective trust is assigned by local admission policy rather than copied from a remote Agent Card. Cards requiring an unavailable credential scheme are refused, and plain HTTP requires an explicit development exception.

Candidate plans remain non-authoritative. Direct delegation is selected only if it is feasible; otherwise bounded candidates are checked and ranked deterministically by task count, auxiliary count, DAG depth, and stable task identifiers. These tests support prototype correctness of the implemented predicates, not mathematical proof of arbitrary plan correctness.

\section{Dependency, Recovery, and Trace Behavior}

Execution tests confirm that a task is dispatched only after every declared prerequisite completes. Failure, timeout, refusal, or unknown outcome skips descendants while independent branches continue. Consumers receive artifacts only from declared dependencies. On takeover, unsafe unknown work is recorded as \texttt{unknown} and is not blindly redispatched. Completion additionally requires the typed completion contract.

Concurrent-session tests show that returned traces contain only persisted events for their session. Authorization precedes attempt start, SDK transport observations are persisted with session, plan, task, and attempt identity, and a trace validator checks ordering and terminal observations. Registration diagnostics remain outside operational session traces.

\section{Corrected End-to-End Harnesses}

The seven deterministic statuses match their references: three successful completions, one authorized runtime failure, and three pre-dispatch infeasibility decisions. The privacy case now derives email and phone values from transcript text; the answers are no longer supplied as separate fixture fields. The task-local auxiliary lifecycle expires after use.

The corrected Docker harness passes eleven checks covering health, registry discovery, direct and decomposed A2A execution, missing capability, insufficient authority, incompatible modes, invalid artifacts, timeout, registry outage, and session replay. The decomposed case originally exposed a stale assumption that every prior artifact should be forwarded. The corrected planner declares dependencies from canonical mode flow, and the harness now verifies that the final consumer receives only its declared predecessor artifact.

The PostgreSQL harness passes five checks: live lease conflict, takeover after an abandoned read-only attempt, process exit after external dispatch of unknown-side-effect work, stale-fence rejection, and database invariant rejection. It records zero duplicate dispatches and repeated effects in these cases, one lease conflict, one stale-write rejection, terminal correctness, and measured recovery latency. These observations do not establish consensus or cover all crash windows.

An additional test launches the vendored, upstream-derived A2A Hello World sample as an independent server, ingests its Agent Card, preserves its \texttt{echo\_bot} skill identifier, authorizes the matching task, and receives its remote artifact. Its provenance records the upstream URL, commit, blob identifiers, copied-file hashes, license, and local comment-only adaptation. This establishes one reproducible interoperability path; it is not evidence of universal A2A compatibility.

\section{Consensus-Backed Distributed Evidence and Open Campaign Gate}

The historical consensus-backed Docker harness starts three unified coordinator containers at a voter target of three with etcd~3.6.13. Its dirty-source report records seven passing checks: three authoritative voters, a cluster-wide registry, conflicting live lease ownership returning HTTP \texttt{409}, rejection of a forged join message, presence of membership history, one terminal result, and zero duplicate external effects in the exercised request. It also records the elected leader, authoritative revision, quorum of two, and discovery paths containing explicit bootstrap and multicast fallback. This is useful development evidence that the topology can form and preserve the checked invariants, but it is not accepted release evidence and does not validate every Raft property.

The new host-side campaign runner automates isolated projects and fresh volumes for formation at three, five, and seven voters; $3\rightarrow5\rightarrow3$ and $5\rightarrow7\rightarrow5$ changes; leader failure; minority and majority partitions; restoration; persistent restart; failed-voter replacement; audit projection failure; concurrent same-session requests; and three crash windows. It records immutable per-trial provenance and refuses accepted status for dirty source. The first local dirty-source smoke attempt preserved three failed reports because Docker could not complete TLS handshakes while resolving the pinned build image; it produced no safety observations and is not counted as a system result. A clean commit and successful full campaign remain required before RQ5 can receive an empirical answer.

The historical status snapshots expose a convergence-observability weakness: one node still labels its local role as \texttt{learner}, and every snapshot shows one pending membership change. The remediated status contract now distinguishes bootstrapping, reconfiguring, ready, degraded, and quorum-lost states; reports configuration generation, authoritative and live voters, learners, leader and revision; includes a durable membership operation and phase; and checks each node's advertised role against both its intent and actual etcd learner/voter role. The new campaign accepts steady state only with role agreement and zero pending operations. This mechanism is unit-tested, but its full container evidence remains open.

\section{Separate Runtime-Only Ablations}

The runtime bundle contains 350 deterministic observations: 100 dependency-dispatch observations, 150 auxiliary-admission observations, and 100 trace-evidence observations. The secure defaults produced zero safety violations across their 150 applicable observations. Removing dependency gating produced 40 unsafe dispatch decisions in the 40 predecessor-missing, failed, timed-out, or unknown observations. Removing auxiliary bounds admitted 40 of 40 deliberately ineligible, persistent, unscoped, or write-authorized specifications; disabling auxiliary behavior admitted none, including ten otherwise valid bounded cases. Suppressing durable diagnostic trace evidence removed the required observation in all 50 trace cases while leaving state-critical persistence unchanged. The observation-file SHA-256 is \texttt{7617bb8da5308b9a\allowbreak 22a7ed7fe12c93d7\allowbreak e0b80484b2a18de2\allowbreak ee44ea3d2e3b7bbf}.

These controls isolate mechanism consequences rather than planning quality. Repetition confirms deterministic behavior of the implemented policy surfaces but does not create 350 independent scenarios: the scenario types, not repeated executions, bound external validity. In particular, the trace result concerns availability of auditable returned evidence, not deletion of leases, fencing tokens, attempts, or terminal results.

\section{Comparative Conformance Study}

The public corpus contains 36 cases and has hash \texttt{b9fd8b6f8398eaf3\allowbreak 3b8676e14288f0d3\allowbreak c4956cca5a9b915c\allowbreak a7b68990167c9c1d}. Its provenance declares author annotation, corpus freeze, and the absence of independent adjudication. The collector verified this exact hash and never included reference labels in prompts. The scorer verified all 540 unique model--seed--case identities before loading labels. Two analysis reruns produced the same SHA-256 digest, \texttt{b70a169d8527b2f4\allowbreak b95a5ef1a7ac083e\allowbreak 0eb81874c896f504\allowbreak 12352b37b14b03a3}. These are leakage, completeness, and reproducibility controls, not evidence that the labels are independently correct.

\begin{table}[htbp]
\centering
\caption{Decision results on 540 repeated observations; intervals are clustered by the 36 cases.}
\label{tab:v03-comparison}
\begin{tabular}{p{0.35\textwidth}rrrr}
\toprule
\textbf{Configuration} & \textbf{Accuracy} & \textbf{95\% CI} & \textbf{FA} & \textbf{FR} \\
\midrule
Hybrid & 58.3\% & 41.7--75.0\% & 0 & 225 \\
Natural-language rule-only & 58.3\% & 41.7--75.0\% & 0 & 225 \\
Structured oracle upper bound & 100.0\% & 100.0--100.0\% & 0 & 0 \\
LLM-only without symbolic gate & 43.9\% & 28.5--59.3\% & 303 & 0 \\
Each planning ablation & 58.3\% & 41.7--75.0\% & 0 & 225 \\
\bottomrule
\end{tabular}
\end{table}

The hybrid refused every feasible observation: precision, recall, and F1 for feasible acceptance were all zero. Its 58.3\% accuracy therefore reflects correct refusal of infeasible cases rather than useful end-to-end acceptance. On feasible observations, failed predicates most often concerned coverage (225), total assignment (191), completion (185), verifiability (147), and ordering (86). The recurring risks show that local-model proposals omitted accountable executors, canonical requirement identities, promised artifacts, completion contracts, or deterministic validation evidence. The symbolic gate behaved fail-closed, but the linguistic-to-typed-plan bridge did not produce executable plans reliably.

The LLM-only configuration achieved 100\% recall but admitted 303 infeasible repeated observations; its feasible-class precision was 42.6\% and F1 was 59.8\%. The structured oracle achieved 100\% on the author-declared criteria, showing that the symbolic evaluator can accept and refuse correctly when supplied the intended structured request and proposal. At the primary 36-case unit, the oracle differed from the hybrid with Holm-adjusted exact McNemar $p=0.000366$; the LLM-only difference was not significant after correction ($p_{\mathrm{Holm}}=1.0$). Rule-only and the three planning ablations had no discordant decisions relative to the hybrid. This floor effect makes those ablations uninformative rather than evidence that their mechanisms do not matter.

Median collected linguistic latency was 4.84~s overall: 3.61~s for Qwen3 1.7B, 4.66~s for Gemma 4 E2B, and 27.39~s for Qwen3 8B. Configurations share these raw outputs, so this latency is not a between-configuration performance comparison. Symbolic-validation and execution latency belong to the separate controlled runtime evidence.

RQ1--RQ3 receive implementation-level support from the corrected boundary and harnesses. The version 0.3 evaluation answers RQ4 negatively for that protocol: symbolic refusal prevented unsafe acceptance, but model-generated plans made the hybrid unusably conservative. This frozen result supports the authorization boundary while falsifying any claim that the original linguistic integration provided effective accepted coordination. It remains limited to the author's declared criteria on 36 designed cases and does not establish general superiority.

\section{Version 0.4 Linguistic-Bridge Redesign}

Version 0.4 preserves version 0.3 as negative evidence and evaluates a new corpus/protocol hash, \texttt{a0ecd2bf0ebc6740\allowbreak a205108e45385eb1\allowbreak e2e7d7ae00dacab3\allowbreak 8bc123c8ca49aa2d}. The model generated only canonical requirement/capability selections and requirement-level dependencies; deterministic hydration supplied executable fields from the admitted request and registry. One bounded structural repair was attempted in 424 of 540 primary outputs. Analysis schema 0.4.1 scores deterministic controls once per case, uses the initial response for LLM-only scoring, and attributes latency to the work performed by each configuration. Two reruns using the calibrated timing bundle produced the same SHA-256 digest, \texttt{16a1faf6b3fff50a\allowbreak ffb8c2a07b9351f3\allowbreak 7dc90d06a8e76640\allowbreak 3b8defbdad96c527}. The raw run records a dirty source tree and is consequently historical rather than accepted release evidence; a clean-source replication remains a release gate.

\begin{table}[htbp]
\centering
\caption{Version 0.4.1 bridge results; LLM configurations have 540 repeated observations, while deterministic controls have 36 case observations.}
\label{tab:v04-comparison}
\begin{tabular}{p{0.37\textwidth}rrrr}
\toprule
\textbf{Configuration} & \textbf{Accuracy} & \textbf{Recall} & \textbf{FA} & \textbf{FR} \\
\midrule
Hybrid constrained bridge & 90.0\% & 76.0\% & 0 & 54 \\
Natural-language rule-only ($n=36$) & 58.3\% & 0.0\% & 0 & 15 \\
Structured oracle upper bound & 100.0\% & 100.0\% & 0 & 0 \\
LLM-only without symbolic gate & 55.4\% & 22.2\% & 66 & 175 \\
Typed-constraint ablation & 89.8\% & 76.0\% & 1 & 54 \\
Authority ablation & 89.8\% & 76.0\% & 1 & 54 \\
Contract-validation ablation & 90.0\% & 76.0\% & 0 & 54 \\
\bottomrule
\end{tabular}
\end{table}

The hybrid's 95\% case-clustered accuracy interval is 85.0--94.6\%. At the case-unit level, all 15 feasible cases had majority acceptance. Nevertheless, only two of three pre-registered success criteria passed. Qwen3 1.7B achieved 28.0\% feasible recall and 70.0\% accuracy; Gemma E2B and Qwen3 8B each achieved 100\% recall and accuracy. Consequently the required 60\% per-model recall floor failed, and version 0.4 is a substantial improvement rather than a fully successful pre-registered validation. The changed corpus wording and bridge protocol also prevent treating the difference from version 0.3 as a direct paired causal estimate.

The corrected latency accounting reports median total latency of approximately 1.91~s for the hybrid and 1.07~s for the initial-response LLM-only configuration; the deterministic lexical and oracle controls require only their local matching or symbolic work. Requested-seed outputs are reported as stability repetitions because the backend response does not confirm seed compliance. Across the five requested seeds, many model--case outputs are identical, so repetition is not interpreted as an increase beyond 36 primary case units.
